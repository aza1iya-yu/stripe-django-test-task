name: stripe-dev

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - 127.0.0.1:8000:8000
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
    restart: unless-stopped